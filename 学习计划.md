# 天机学堂 · 微服务学习计划（小白版）

> 面向：有计算机四大件基础、但没做过完整项目的同学。
> 目标：**真真实实学到架构 + 补代码能力 + 理解一个功能从点击到落库的完整运行链路**。
> 依据：本仓库真实代码 + [`天机学堂-扩展.md`](天机学堂-扩展.md)（46 万字改造笔记，记录每个 bug 与功能的技术考量与取舍）。

---

## 怎么用这份计划

- **不要一上来全跑 / 全读**。18 个服务模块 + 一堆中间件，正确打开方式是「单服务内部 → 跨服务链路 → 一个旗舰功能全链路」逐层击破。
- 每层都有 **学习目标 → 真实文件入口 → 动手任务 → 关联笔记章节 → 自测题（附考察点）**。
- 自测题先自己答，答完对照「考察点」自检；建议把答案发给 AI 判卷补漏。
- 推荐执行顺序：第 0 层自测 → 第 1 层（核心）→ 第 2 层 → 第 3 层（高潮）→ 第 4 层（按需）。

---

## 第 0 层 · 骨架与心智模型（1~2 天，只读不写）

### 学习目标
不被 18 个模块吓住，建立「微服务 = 按业务域拆分的多个独立 Spring Boot 工程」的心智模型。

### 真实文件入口
- [pom.xml](pom.xml) — 看两处：`<modules>`（聚合了哪些子工程）、`<dependencyManagement>`（怎么统一锁版本）。
- [README.md](README.md) 的「模块介绍」「技术栈」「解决方案」三节。
- [nacos/](nacos/) — 每个服务一份 yml，理解配置外置。
- [天机学堂-扩展.md](天机学堂-扩展.md) 的 `# 启动流程总结&配置疑问`（第 9 行起）和 `## 技术配置 / 网关怎么走的`（第 304 行起）。

### 动手任务
1. 在纸上画一张图：把 18 个模块分成三类（入口类 / 业务类 / 支撑类），标出谁依赖谁。
   - 提示：所有业务模块都依赖 [tj-common](tj-common/) 和 [tj-api](tj-api/)。
2. 读 [天机学堂-扩展.md](天机学堂-扩展.md) 的「启动流程」一节，把后端启动顺序记下来。

### 模块分类速记
| 类别 | 模块 |
| --- | --- |
| 入口类 | [tj-gateway](tj-gateway/)（网关）、[tj-auth](tj-auth/)（鉴权） |
| 业务类 | tj-user / tj-course / tj-learning / tj-trade / tj-pay / tj-promotion / tj-exam / tj-message / tj-search / tj-remark / tj-media / tj-data / tj-live / tj-chat |
| 支撑类 | [tj-common](tj-common/)（公共工具）、[tj-api](tj-api/)（跨服务调用契约） |

### 自测题
1. **为什么这个项目不做成一个大的 Spring Boot 单体工程，而要拆成 18 个微服务？至少说出 3 条理由。**
   - *考察点*：业务域拆分 / 独立部署 / 团队边界 / 技术异构 / 故障隔离，而不是只会背"为了高并发"。
2. **`tj-common` 和 `tj-api` 都是"公共模块"，它们的职责有什么不同？如果把 tj-api 里的东西塞进 tj-common 会怎样？**
   - *考察点*：通用工具（tj-common）与对外服务契约/Feign 接口（tj-api）的区别，以及依赖方向。
3. **父 pom 里 `<dependencyManagement>` 和直接在 `<dependencies>` 里写依赖，区别是什么？为什么 lombok、test 写在 `<dependencies>`，而 redisson、mybatis-plus 写在 `<dependencyManagement>`？**
   - *考察点*：Maven 依赖管理基本功——版本锁定 vs 实际引入。
4. **Nacos 在这个项目里同时承担了哪两个角色？如果 Nacos 挂了，服务还能启动吗？为什么？**
   - *考察点*：注册中心（服务发现）+ 配置中心双职责；bootstrap.yml 先于 application.yml 加载 Nacos 配置。

---

## 第 1 层 · 单服务内部四层结构（核心，1~2 周）

### 学习目标
**这是补代码能力的主战场。** 微服务再花哨，单个服务内部永远是 `Controller → Service → Mapper → DB`。先吃透这一条，再碰跨服务的东西。

### 服务选择
锁定 **[tj-promotion](tj-promotion/)（营销中心 · 优惠券）**。业务直观、链路干净，且是后面第 3 层"秒杀旗舰功能"的载体。

### 真实文件入口（按调用顺序读）
1. [CouponController.java](tj-promotion/src/main/java/com/tianji/promotion/controller/CouponController.java) — 接口入口。
2. 同目录 `service/CouponService`（接口）+ `service/impl/CouponServiceImpl`（实现）— 业务逻辑。
3. `mapper/CouponMapper` + `src/main/resources/.../*.xml` — SQL 执行层（MyBatis-Plus）。
4. `entity/po/Coupon`（数据库对象）/ `entity/dto/...`（传输对象）/ `entity/vo/...`（视图返回）— 三种模型为什么分开。
5. [tj-common](tj-common/) 里翻 `Result` 统一返回类、全局异常处理器、分页工具——所有服务共享的"地基"。

### 动手任务（写会，不是看会）
1. **精读一条链路**：`新增优惠券` 接口从 Controller 到落库的全过程，逐行注释每个注解的作用（`@RestController` `@Api` `@PostMapping` `@Valid` `@Transactional` `@Cache`…）。
2. **自己加一个全新接口**：`查询某用户所有优惠券`，完整实现 Controller→Service→Mapper，用 [tj-common](tj-common/) 的 `PageQuery` 分页返回。
3. 学透三个基础概念并在代码里指认：IoC（`@Autowired`/构造器注入）、AOP（找一个切面）、声明式事务（`@Transactional` 的传播行为）。

### 关联笔记
[天机学堂-扩展.md](天机学堂-扩展.md) `## 交易微服务 / 结算因为没优惠券出错`（第 541 行）— 看一个真实优惠券 bug 是怎么排查修复的。

### 自测题
1. **一个 HTTP 请求 `POST /coupons` 到达后，从 Tomcat 接收 socket 到执行完 SQL 返回，中间经过了哪些层/组件？请按顺序列出至少 6 个环节。**
   - *考察点*：DispatcherServlet → Filter → 拦截器 → Controller → Service（事务边界）→ Mapper → 数据库。
2. **`@Transactional` 加在 Controller 上和加在 Service 实现类的方法上，效果一样吗？哪种是正确做法？如果一个 Service 方法里调了本类的另一个 `@Transactional` 方法，内层事务会生效吗？**
   - *考察点*：事务应放在业务层；Spring AOP 自调用失效（this 代理穿透）。
3. **PO / DTO / VO 为什么要分成三类？能不能用一个实体类从头用到尾？这样做的代价是什么？**
   - *考察点*：分层模型隔离变化、字段权限（不暴露敏感字段）、序列化控制。
4. **MyBatis-Plus 里 `BaseMapper<T>` 自带的 `selectById` 是怎么变成 SQL 的？如果你要写一个多表 join 的复杂查询，应该用 XML 还是 `@Select` 注解？为什么这个项目两种都用？**
   - *考察点*：MP 反射生成 SQL vs 手写 SQL 的取舍边界。
5. **[tj-common](tj-common/) 的统一返回类 `Result` 是怎么做到所有 Controller 都不用手写 `try/catch` 也能返回规范结构的？背后的机制是什么？**
   - *考察点*：`@RestControllerAdvice` + `@ExceptionHandler` 全局异常处理 + `ResponseBody` 包装。

---

## 第 2 层 · 跨服务链路（微服务精髓，1~2 周）

### 学习目标
单服务练熟后才轮到这里。理解微服务真正值钱的部分：**请求怎么进来、怎么找到目标服务、怎么互相调用、怎么异步解耦。**

### 真实文件入口（按一条真实请求链路读）
> 场景：用户点"领优惠券" → 网关鉴权 → 路由到 tj-promotion → 可能调 tj-user 查用户 → 写 Redis + 发 MQ → 返回。

1. **网关层** [tj-gateway/src/main/java/com/tianji/gateway/filter/](tj-gateway/src/main/java/com/tianji/gateway/filter/)
   - [AccountAuthFilter.java](tj-gateway/src/main/java/com/tianji/gateway/filter/AccountAuthFilter.java) — 鉴权
   - [LogTrackingFilter.java](tj-gateway/src/main/java/com/tianji/gateway/filter/LogTrackingFilter.java) — 日志追踪
   - [RequestIdRelayFilter.java](tj-gateway/src/main/java/com/tianji/gateway/filter/RequestIdRelayFilter.java) — 请求 ID 透传
2. **服务发现/配置**：各服务 `bootstrap.yml` 里的 nacos 配置。
3. **跨服务调用** [tj-api](tj-api/) — 整个项目最体现架构思维的设计。每个服务把能力暴露成接口放这里，别服务用 **OpenFeign** 声明式调用。找一个 `@FeignClient` 的例子。
4. **异步解耦**：在 tj-promotion 里 grep `RabbitTemplate` 或 `@RabbitListener`，看一处发消息/收消息。

### 动手任务
1. 画出"领优惠券"的完整时序图（前端→网关→promotion→user/Redis/MQ→返回），标出每一步同步还是异步。
2. 在 [tj-api](tj-api/) 里给某个服务**新增一个 Feign 接口**并在另一个服务里调用跑通。

### 关联笔记
[天机学堂-扩展.md](天机学堂-扩展.md)：
- `## 技术配置 / 网关怎么走的`（第 304 行）
- `### 如何让新的微服务获取用户信息`（第 385 行）
- `### 如何后端开放一个不用鉴权也可以访问的路径`（第 379 行）

### 自测题
1. **Gateway 里的 `GlobalFilter` 和 `GatewayFilter` 有什么区别？[AccountAuthFilter](tj-gateway/src/main/java/com/tianji/gateway/filter/AccountAuthFilter.java) 为什么放在网关而不是放在每个业务服务里做？**
   - *考察点*：全局 vs 路由级过滤器；统一鉴权 vs 重复实现的取舍。
2. **用户登录后，后续请求是怎么知道"我是谁"的？从网关到业务服务，用户身份（userId）是怎么传递下去的？业务服务里的用户信息又是怎么取到的？**
   - *考察点*：JWT 校验 → Header 透传 → 业务侧拦截器/ThreadLocal 取 userId 的链路。
3. **OpenFeign 调用另一个服务时，请求其实是 HTTP 调用。如果被调服务挂了或响应慢，调用方会怎样？这个项目有没有做容错处理？应该怎么做？**
   - *考察点*：同步调用的超时/熔断问题，引出 Sentinel（第 4 层）。
4. **为什么有些操作用 OpenFeign 同步调，有些却用 RabbitMQ 异步发？请各举一例，并说明判断依据。**
   - *考察点*：需不需要立即拿到结果 / 解耦削峰 / 容错重试的取舍。
5. **为什么要有 [tj-api](tj-api/) 这个独立模块，而不是每个服务自己定义自己的 Feign 客户端？如果 A 服务直接依赖 B 服务的 jar 包来调 Feign，会有什么问题？**
   - *考察点*：契约共享 + 避免循环依赖 + 避免把实现也暴露出去。

---

## 第 3 层 · 旗舰功能全链路（高潮，2~3 周）

### 学习目标
"理解一个功能的完美实现"就靠这一层。**只挑 1 个吃透**，不要全看。下面三个任选其一，按推荐度排序。

### 选项 A（🏆 强推）· 优惠券秒杀：Lua + Redis 分布式锁
- **入口** [tj-promotion](tj-promotion/) 的 [UserCouponController.java](tj-promotion/src/main/java/com/tianji/promotion/controller/UserCouponController.java)、`ExchangeCodeController`，追到领取逻辑里的 Redis Lua 脚本。
- README 标注："基于 LUA 脚本的高性能、并发安全优惠券领取解决方案（秒杀）"。
- **关联笔记** [天机学堂-扩展.md](天机学堂-扩展.md) 优惠券相关 bug 段落。

### 选项 B · 视频学习进度：Redis 合并写 + 定时任务持久化
- **入口** [LearningRecordController.java](tj-learning/src/main/java/com/tianji/learning/controller/LearningRecordController.java)。
- README："基于 Redis 合并写请求并基于定时任务异步持久化的并发优化方案"。
- **关联笔记** [天机学堂-扩展.md](天机学堂-扩展.md) `## 学习微服务`（第 708 行起）一整节，bug 极多，实战价值高。

### 选项 C · 订单状态机
- **入口** [tj-trade](tj-trade/)。
- **关联笔记** [天机学堂-扩展.md](天机学堂-扩展.md) `## 使用Spring状态机优化订单流转`（第 7757 行）——手把手讲取舍，强烈建议配合读。

### 动手任务（三个选项通用）
1. 把所选功能从**前端点击 → 网关 → 业务服务 → Redis/MQ/定时任务 → 落库**的完整时序图画出来。
2. 找到那条链路里**最关键的并发代码**（Lua 脚本 / 分布式锁 / 合并写），逐行搞懂"它在防什么并发问题"。
3. 自己设计一个"破坏性测试"：模拟 100 个并发请求同时领同一张券，观察结果是否正确。

### 自测题（按选项 A，秒杀）
1. **秒杀场景下，为什么不能直接 `select 库存 → 判断 >0 → update 库存-1 → 插入领券记录`？这段代码在 100 并发下会发生什么？**
   - *考察点*：超卖问题、读改写的竞态、数据库行锁。
2. **用 Redis Lua 脚本做库存扣减，相比直接用 Redis 的 `DECR` 命令，优势在哪？为什么非要用 Lua？**
   - *考察点*：原子性（判断+扣减+记录多步合一）、避免多次网络往返、脚本原语执行不被打断。
3. **Redisson 分布式锁和 `synchronized` / `ReentrantLock` 有什么本质区别？如果持有锁的服务实例突然宕机了，锁会怎样？这个项目怎么解决？**
   - *考察点*：本地锁 vs 跨进程锁；看门狗续期 + 过期时间防死锁。
4. **Redis 扣减库存成功了，但接下来写 MySQL 失败了，数据不一致怎么办？这个项目是怎么兜底的？（提示：MQ + 重试 + 对账）**
   - *考察点*：最终一致性思想，引出可靠消息方案。
5. **秒杀高峰流量打过来，如果连 Redis 都扛不住，前面还能加什么挡一层？**
   - *考察点*：Nginx 限流 → 网关限流 → Sentinel → 缓存预热 → 异步化，分层防御。

---

## 第 4 层 · 高可用与运维（按需，可选）

### 学习目标
等 1~3 层扎实了再碰，否则只是背名词。

### 真实入口 + 关联笔记
- **Sentinel 限流**：[天机学堂-扩展.md](天机学堂-扩展.md) `## Sentinel引入`（第 7710 行）。
- **XXL-JOB 定时任务**：[xxl-job/](xxl-job/) 目录 + `sql/xxl_job.sql`。
- **分布式事务 Seata**：根 pom 已引入 seata-version。
- **可观测**：InfluxDB 埋点、Prometheus+Grafana（README 技术栈）。

### 自测题
1. **Sentinel 和 Hystrix 都做熔断限流，思路有什么不同？Sentinel 的"热点参数限流"在秒杀场景能用来干什么？**
2. **XXL-JOB 的调度中心和工作节点（执行器）是什么关系？如果调度中心挂了，已调度的任务还会执行吗？**
3. **Seata 的 AT 模式是怎么做到"跨服务事务回滚"的？它靠什么记录回滚需要的信息？为什么这种模式对性能有影响？**
4. **网关日志这个项目是怎么做的？为什么要用"网关全局过滤器 + Redis + MQ"三层而不是直接写库？（README 解决方案最后一条）**

---

## 跑起来验证（最小子集）

不用在服务器全量部署。最小可运行：

1. Docker 起 **MySQL + Redis + Nacos** 三个容器（部署说明见 [天机学堂-扩展.md](天机学堂-扩展.md)）。
2. 导 [sql/test](sql/test/) 带数据表。
3. 只启动 **tj-gateway + tj-auth + tj-promotion** 三个服务。
4. 用 Knife4j（Swagger）页面直接调接口。
5. 关键方法打断点，**单步调试**看请求怎么一层层走——这是理解"运行链路"最有效的方式，比读十遍代码都管用。

---

## 关键概念索引（学到哪查到哪）

| 概念 | 出现层 | 代码位置 |
| --- | --- | --- |
| IoC / DI | 第 1 层 | 所有 `@Autowired` / 构造器注入 |
| AOP / 切面 | 第 1 层 | [tj-common](tj-common/) 切面 |
| 声明式事务 | 第 1 层 | Service 层 `@Transactional` |
| 全局异常处理 | 第 1 层 | [tj-common](tj-common/) `@RestControllerAdvice` |
| 网关过滤器链 | 第 2 层 | [tj-gateway/filter](tj-gateway/src/main/java/com/tianji/gateway/filter/) |
| JWT 鉴权 + 用户透传 | 第 2 层 | [AccountAuthFilter](tj-gateway/src/main/java/com/tianji/gateway/filter/AccountAuthFilter.java) |
| OpenFeign 跨服务调用 | 第 2 层 | [tj-api](tj-api/) `@FeignClient` |
| RabbitMQ 异步 | 第 2 层 | 各服务 `RabbitTemplate` / `@RabbitListener` |
| Redis Lua 秒杀 | 第 3 层 | [tj-promotion](tj-promotion/) 领券逻辑 |
| Redisson 分布式锁 | 第 3 层 | 自定义注解 + Redisson |
| Redis 合并写 + 定时持久化 | 第 3 层 | [tj-learning](tj-learning/) 学习记录 |
| Spring 状态机订单 | 第 3 层 | [tj-trade](tj-trade/) |
| Sentinel 限流熔断 | 第 4 层 | 笔记第 7710 行 |
| XXL-JOB 定时调度 | 第 4 层 | [xxl-job/](xxl-job/) |
| Seata 分布式事务 | 第 4 层 | 根 pom seata-version |

---

> 下一步建议：今天就从**第 0 层自测题**开始，答完发我判卷；或直接说「跳到第 1 层，带我读优惠券代码」，从真实代码开始逐行精读。
